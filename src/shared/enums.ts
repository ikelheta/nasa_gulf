export enum AdminStatus {
  ACTIVE = "0",
  SUSPENDED = "1",
}

export enum Gender {
  MALE,
  FEMALE,
  OTHER,
}

export enum SystemUserTypes {
  Admin = 'Admin',
  Employee = 'Employee'
}
export enum ProjectRequestTypes {
  WIR = 'WIR',
  MTR = 'MTR',
  DSR = 'DSR',
  MSR = 'MSR'
}

